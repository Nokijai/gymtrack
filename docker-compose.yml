version: '3.8'

services:
  backend:
    image: ghcr.io/nokijai/gymtrack-backend:latest
    ports:
      - "5000:8000"
    volumes:
      - gymtrack-data:/app/data
    environment:
      - SECRET_KEY=${SECRET_KEY:-gymtrack-secret-change-in-prod-2024}
    restart: unless-stopped

  frontend:
    image: ghcr.io/nokijai/gymtrack-frontend:latest
    ports:
      - "5001:3000"
    restart: unless-stopped
    depends_on:
      - backend

volumes:
  gymtrack-data:
